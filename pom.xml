<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>2.6.9</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>tn.esprit.spring</groupId>
	<artifactId>gestion-station-ski</artifactId>
	<version>1.0</version>
	<name>gestion-station-ski</name>
	<description>Demo project for Spring Boot</description>

	<!-- Specify finalName to control the output jar name -->
	<build>
		<finalName>gestion-station-ski-${revision}</finalName> <!-- You can replace ${revision} with Git commit ID dynamically in Jenkins -->
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>3.1.0</version> <!-- Specify version if not provided -->
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.22.2</version>
				<configuration>
					<skipTests>false</skipTests>
				</configuration>
			</plugin>
			<!-- Optional: Add the Maven Deploy Plugin -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<version>2.8.2</version>
				<executions>
					<execution>
						<goals>
							<goal>deploy-file</goal>
						</goals>
						<phase>deploy</phase>
						<configuration>
							<file>${project.build.directory}/gestion-station-ski-${revision}.jar</file> <!-- Make sure the jar file exists -->
							<repositoryId>deploymentRepo</repositoryId>
							<url>http://192.168.50.4:8081/repository/maven-releases/</url>
							<pomFile>${project.basedir}/pom.xml</pomFile>
							<groupId>tn.esprit.spring</groupId>
							<artifactId>gestion-station-ski</artifactId>
							<version>1.0-${revision}</version> <!-- Use dynamic version based on Git commit ID -->
							<packaging>jar</packaging>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<properties>
		<java.version>11</java.version>
		<maven-jar-plugin.version>3.1.1</maven-jar-plugin.version>
		<!-- Define a Git commit ID or another dynamic version identifier -->
		<revision>${env.GIT_COMMIT}</revision> <!-- For Jenkins, inject the Git commit ID dynamically -->
	</properties>

	<dependencies>
		<!-- Your dependencies here -->
	</dependencies>

	<distributionManagement>
		<repository>
			<id>deploymentRepo</id>
			<name>Nexus Repository</name>
			<url>http://192.168.50.4:8081/repository/maven-releases/</url>
		</repository>
	</distributionManagement>

</project>
